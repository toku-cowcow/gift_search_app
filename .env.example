# =============================================================================
# UchiGift - 冠婚葬祭ギフト検索アプリ 環境変数設定テンプレート
# =============================================================================
# 
# 使い方:
# 1. このファイルをコピーして .env という名前で保存
# 2. 必要な値を記入（開発環境ではデフォルト値のままでOK）
# 3. 本番環境では全てのコメントアウト箇所を適切に設定
#
# ⚠️ 注意: .env ファイルは絶対にGitにコミットしないこと

# =============================================================================
# 現在の開発環境で必要な設定
# =============================================================================

# Meilisearch接続設定（検索エンジン）
MEILI_URL=http://127.0.0.1:7700
MEILI_KEY=masterKey
INDEX_NAME=items

# アプリケーションURL
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8000

# CORS設定（カンマ区切りで複数指定可能）
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# タイムゾーン
TIMEZONE=Asia/Tokyo

# =============================================================================
# データソース切り替え設定（将来の拡張用）
# =============================================================================

# フロントエンドのデータ取得方法
# mock: モックデータを使用（現在の設定、開発・テスト用）
# api: バックエンドAPIを呼び出し（将来の本格運用時）
NEXT_PUBLIC_DATA_SOURCE=mock

# =============================================================================
# 将来の楽天API連携用設定（現在は未使用）
# =============================================================================

# 楽天市場API認証情報
# RAKUTEN_APPLICATION_ID=your-rakuten-app-id-here
# RAKUTEN_AFFILIATE_ID=your-affiliate-id-here

# 楽天API設定
# RAKUTEN_API_ENDPOINT=https://app.rakuten.co.jp/services/api/IchibaItem/Search/20220601
# RAKUTEN_API_RATE_LIMIT=1000

# =============================================================================
# 将来のAI機能用設定（現在は未使用）
# =============================================================================

# OpenAI API（商品レコメンド、チャットボット用）
# OPENAI_API_KEY=sk-your-openai-api-key-here
# OPENAI_MODEL=gpt-3.5-turbo
# OPENAI_MAX_TOKENS=500

# =============================================================================
# 本番環境用セキュリティ設定（現在は未使用）
# =============================================================================

# セキュリティキー（本番環境では必須）
# SECRET_KEY=your-super-secret-key-change-this-in-production
# JWT_SECRET=your-jwt-secret-for-authentication

# データベース接続（本番環境で商品データをDB管理する場合）
# DATABASE_URL=postgresql://user:password@localhost/uchigift_prod

# 外部サービス連携
# SENTRY_DSN=your-sentry-dsn-for-error-tracking
# GOOGLE_ANALYTICS_ID=GA-XXXXXXXXXX

# =============================================================================
# 本番環境用パフォーマンス・ログ設定（現在は未使用）
# =============================================================================

# ログレベル設定
# LOG_LEVEL=INFO
# ENABLE_DEBUG_MODE=false

# キャッシュ設定
# REDIS_URL=redis://localhost:6379
# CACHE_TTL_SECONDS=3600

# パフォーマンス監視
# ENABLE_METRICS=true
# METRICS_ENDPOINT=/metrics

# =============================================================================
# デプロイメント環境設定（現在は未使用）
# =============================================================================

# 環境識別子
# DEPLOYMENT_ENVIRONMENT=production
# APP_VERSION=1.0.0

# SSL/TLS設定
# FORCE_HTTPS=true
# SSL_CERT_PATH=/path/to/ssl/cert.pem
# SSL_KEY_PATH=/path/to/ssl/key.pem

# =============================================================================
# 設定完了後の確認項目
# =============================================================================
#
# ✅ Meilisearchが起動している (docker compose up -d)
# ✅ バックエンドが起動している (python app/main.py)
# ✅ フロントエンドが起動している (pnpm dev)
# ✅ http://localhost:3000 でアクセス可能
# ✅ 商品検索が動作する
#